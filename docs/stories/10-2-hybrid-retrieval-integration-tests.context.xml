<storyContext>
  <story id="10.2" title="Hybrid Retrieval Integration Tests" epic="10" />

  <summary>
    Add integration tests that validate vector search, graph traversal,
    and evidence payloads with real Postgres and Neo4j services.
  </summary>

  <requirements>
    <acceptanceCriteria>Vector and graph evidence returned for hybrid retrieval.</acceptanceCriteria>
    <acceptanceCriteria>Graph traversal returns expected paths.</acceptanceCriteria>
    <acceptanceCriteria>Tenant isolation verified in retrieval.</acceptanceCriteria>
  </requirements>

  <constraints>
    <constraint>Tests must use integration fixtures from Story 10.1.</constraint>
  </constraints>

  <references>
    <reference path="docs/epics/epic-10-tech-spec.md" section="Story Breakdown" />
    <reference path="docs/epics/epic-10-testing-infrastructure.md" section="Story 10.2" />
  </references>
</storyContext>
