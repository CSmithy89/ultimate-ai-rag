<storyContext>
  <story id="3.2" title="Graph Relationship Traversal" epic="3" />

  <summary>
    Add Neo4j traversal for relationship-based queries and expose nodes,
    edges, and paths with tenant isolation.
  </summary>

  <requirements>
    <acceptanceCriteria>Identify relevant starting entities from query terms.</acceptanceCriteria>
    <acceptanceCriteria>Traverse relationships via Cypher.</acceptanceCriteria>
    <acceptanceCriteria>Return connected entities and relationship paths.</acceptanceCriteria>
    <acceptanceCriteria>Enforce tenant isolation in queries.</acceptanceCriteria>
  </requirements>

  <constraints>
    <constraint>Traversal must be bounded by max hops and max paths.</constraint>
    <constraint>Use allowed relationship types only.</constraint>
  </constraints>

  <references>
    <reference path="backend/src/agentic_rag_backend/db/neo4j.py" section="Neo4jClient" />
    <reference path="docs/epics/epic-3-tech-spec.md" section="Story 3.2" />
  </references>
</storyContext>
