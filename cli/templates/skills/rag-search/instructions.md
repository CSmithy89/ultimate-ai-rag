# RAG Search Skill

This skill searches the knowledge graph and vector store to find relevant information.

## Usage

Ask questions naturally. The skill will:
1. Select retrieval strategy
2. Query vector + graph stores
3. Return results with sources

## Configuration

Ensure the backend is running at `http://localhost:8000`.
